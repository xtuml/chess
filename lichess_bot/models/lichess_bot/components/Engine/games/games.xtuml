// BP 7.1.6 content: Package syschar: 3 persistence-version: 7.1.6

within lichess_bot::components::Engine is

  package games is
    class 'Active Game';
    class Player is lichess_bot::components::Engine::board::Player;
    //! This modeled game automates the decision making for one player.
    relationship R22 is 'Active Game' unconditionally 'is acting for' one Player,
                        Player unconditionally 'delegates to' one 'Active Game';

  end package;

end;
